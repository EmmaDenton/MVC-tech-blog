
{{#each posts}}
    <div class="post">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <p>Posted on: {{formatDate this.createdAt}}</p> <!-- Assuming you have a helper to format dates -->
    </div>
{{/each}}

</div>
    <div>
        <form id="logout-form">
            <button type="submit" id="logout-button">Logout</button>
        </form>
    </div>
<script src="/js/logout.js"></script>